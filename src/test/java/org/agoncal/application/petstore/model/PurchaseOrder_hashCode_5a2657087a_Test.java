// Test generated by RoostGPT for test JavyaUnitTesting using AI Type Open AI and AI Model gpt-4

package org.agoncal.application.petstore.model;

import org.junit.Before;
import org.junit.Test;
import static org.junit.Assert.*;

public class PurchaseOrder_hashCode_5a2657087a_Test {
    private PurchaseOrder order1, order2;

    @Before
    public void setUp() {
        order1 = new PurchaseOrder();
        order1.setOrderDate(new Date());
        order1.setCustomer(new Customer());

        order2 = new PurchaseOrder();
        order2.setOrderDate(new Date());
        order2.setCustomer(new Customer());
    }

    @Test
    public void testHashCode_sameInstance() {
        // Same instance should always have same hashCode
        assertEquals(order1.hashCode(), order1.hashCode());
    }

    @Test
    public void testHashCode_differentInstance() {
        // Different instances with same values should have same hashCode
        order2.setOrderDate(order1.getOrderDate());
        order2.setCustomer(order1.getCustomer());
        assertEquals(order1.hashCode(), order2.hashCode());
    }

    @Test
    public void testHashCode_differentValues() {
        // Different instances with different values should have different hashCode
        assertNotEquals(order1.hashCode(), order2.hashCode());
    }
}
