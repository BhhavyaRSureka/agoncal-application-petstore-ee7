// Test generated by RoostGPT for test JavyaUnitTesting using AI Type Open AI and AI Model gpt-4

package org.agoncal.application.petstore.view;

import org.junit.Before;
import org.junit.Test;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import javax.faces.application.FacesMessage;
import javax.faces.context.FacesContext;

import static org.mockito.Mockito.*;

public class AbstractBean_addErrorMessage_c5f6545bb7_Test {

    @Mock
    private FacesContext facesContext;

    @Mock
    private FacesMessage facesMessage;

    @InjectMocks
    private AbstractBean abstractBean;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
    }

    @Test
    public void testAddErrorMessage() {
        when(facesContext.getCurrentInstance()).thenReturn(facesContext);
        abstractBean.addErrorMessage("Test message", "Test1", "Test2");
        verify(facesContext, times(1)).addMessage(anyString(), any(FacesMessage.class));
    }

    @Test(expected = NullPointerException.class)
    public void testAddErrorMessage_NullContext() {
        when(facesContext.getCurrentInstance()).thenReturn(null);
        abstractBean.addErrorMessage("Test message", "Test1", "Test2");
    }
}
