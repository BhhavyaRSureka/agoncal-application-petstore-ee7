// Test generated by RoostGPT for test JavyaUnitTesting using AI Type Open AI and AI Model gpt-4

package org.agoncal.application.petstore.view;

import javax.faces.context.FacesContext;
import javax.faces.context.ExternalContext;
import org.junit.Test;
import org.junit.Before;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

import java.util.HashMap;
import java.util.Map;

import static org.junit.Assert.*;
import static org.mockito.Mockito.*;

public class AbstractBean_getParam_af741b4b12_Test {

    @Mock
    private FacesContext facesContext;

    @Mock
    private ExternalContext externalContext;

    private AbstractBean abstractBean;

    @Before
    public void setup() {
        MockitoAnnotations.openMocks(this);
        when(facesContext.getExternalContext()).thenReturn(externalContext);
        abstractBean = new AbstractBean();
    }

    @Test
    public void testGetParamSuccess() {
        Map<String, String> params = new HashMap<>();
        params.put("test", "value");
        when(externalContext.getRequestParameterMap()).thenReturn(params);

        String result = abstractBean.getParam("test");
        assertEquals("value", result);
    }

    @Test
    public void testGetParamNotFound() {
        Map<String, String> params = new HashMap<>();
        when(externalContext.getRequestParameterMap()).thenReturn(params);

        String result = abstractBean.getParam("test");
        assertNull(result);
    }
}
